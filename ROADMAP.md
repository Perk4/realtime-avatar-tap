# Roadmap

Web-apps track for `realtime-avatar-tap`. The four-primitive teach API stays on the path. This is not a LemonSlice clone.

## Done

- Teach tap: `openSession`, `ingestAudioChunk`, `emitAvatarBlock`, `assertContinuous` (#1).

## Now

- Potato demo on that tap: mic or fixture WAV, visible canvas avatar, recorded artifact in-repo (#2).

## Later

- Optional live LLM reply into the same session. Not required for v1.
- Out of scope: GPU renderer, live WebRTC product.
